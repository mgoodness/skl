# Baz

A skill named baz, nested a level deeper than foo/bar under a "group"
directory grouping (directory groups are out of scope for this ticket;
this fixture only exercises destination flattening: the installed name
must be "baz", not "group/baz" or "group-baz").
