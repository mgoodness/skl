# too-deep

A skill one level past the depth-3 boundary (skills/at-depth-three/only/here/too-deep), which the depth-3 walk must NOT find, even though tier 3 already succeeded (found "here") and so never falls back to the unbounded search that would otherwise find this one too.
